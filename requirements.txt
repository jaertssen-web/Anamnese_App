# Anamnese_App — Python dependencies
#
# Installeren (automatisch via bootstrap_and_run.sh):
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Systeemvereisten buiten pip:
#   brew install ffmpeg    # vereist door openai-whisper voor audio-decodering
#   macOS mic-toestemming voor Terminal/Python (voor live opname)

# UI — st.fragment(run_every=) vereist ≥ 1.37 (live transcriptie)
streamlit>=1.37

# Lokale Whisper-transcriptie (offline, geen LLM-server nodig)
# Modellen worden automatisch gedownload bij eerste gebruik (~74 MB voor base)
openai-whisper>=20231117

# Live audio-opname (v3.0 — chunked live transcriptie)
# sounddevice: cross-platform InputStream via PortAudio
# soundfile:   WAV schrijven vanuit numpy array
# numpy:       audio-buffers
sounddevice>=0.4.6
soundfile>=0.12
numpy>=1.24
